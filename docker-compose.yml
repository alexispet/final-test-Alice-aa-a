services:
  express:
    build:
      context: .
      #      choose image : next
      target: express
    environment:
      #      stable or unstable
      NODE_ENV: development
      MYSQL_RANDOM_ROOT_PASSWORD: yes
      MYSQL_DATABASE: express
      MYSQL_USER: express
      MYSQL_PASSWORD: express
    ports:
      - "3000:3000"
    depends_on:
      db:
        condition: service_healthy
    volumes:
      - ".:/app"
    command: [ "npm", "run", "start" ]
  db:
    image: mysql
    environment:
      MYSQL_RANDOM_ROOT_PASSWORD: no
      MYSQL_DATABASE: express
      MYSQL_USER: express
      MYSQL_PASSWORD: express
    volumes:
      - "./docker/mariadb/data:/var/lib/mariadb"
    ports:
      - 3000:3300
    healthcheck:
      test: "/usr/bin/mysql --user=express --password=express --execute \"SHOW DATABASES;\""
      interval: 1m30s
      timeout: 30s
      retries: 20
      start_period: 30s
  adminer:
    image: adminer
    ports:
      - 8080:8080